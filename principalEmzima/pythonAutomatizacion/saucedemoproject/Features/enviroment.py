# from imports import pytest,webdriver 

# def before_all(context):
#     """Conecta el fixture de navegador a Behave."""
#     context.driver = webdriver.Chrome()  # O usa el fixture si es compatible
#     context.driver.maximize_window()
#     # context.driver = driver

# def after_all(context):
#     """Cierra el navegador al final de todas las pruebas."""
#     context.driver.quit()